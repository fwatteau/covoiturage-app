import {ILatLng} from "./latLng.interface";
import {LatLngBounds} from "leaflet";

export class Parent {
    id: number;
    name: string;
    children: string;
    classroom: number;
    address: string;
    mail: string;
    phone: string;
    needs: string;
    capacities: string;
}
